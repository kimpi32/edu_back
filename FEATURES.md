# MathLMS - 기능 가이드

> 문제 시퀀스로 수학 개념을 체득하는 학습 관리 시스템

---

## 접속 정보

```
http://localhost:3001
```

로그인 페이지에서 **테스트 로그인** 버튼으로 각 역할을 체험할 수 있습니다.

---

## 역할별 기능 요약

| 역할 | 진입점 | 할 수 있는 것 |
|------|--------|--------------|
| 👨‍🏫 교사 | `/dashboard` | 반 관리, 시퀀스 배정, 학생 분석 |
| 👨‍🎓 학생 | `/learn` | 문제 풀기, 오답 복습, 내 진도 확인 |
| ⚙️ 관리자 | `/admin/concepts` | 개념·문제·시퀀스 CRUD |

---

## 1. 공통 페이지

### `/` 랜딩 페이지
- 서비스 소개 (히어로, 예시 시퀀스, 기능 설명)
- 교사/학생 역할 안내
- 로그인·회원가입 버튼

### `/login` 로그인
- **테스트 로그인**: 교사 / 학생 / 관리자 버튼 클릭으로 즉시 진입
- 이메일 + 비밀번호 로그인 (Supabase 연동 시)

### `/signup` 회원가입
- 이름, 이메일, 비밀번호 입력
- 교사/학생 역할 선택

---

## 2. 관리자 페이지 (⚙️ 관리자로 로그인)

### `/admin/concepts` 개념 트리 관리
```
수와 연산 (대)
├── 자연수 사칙연산 (중)
│   ├── 덧셈 원리 (소)
│   ├── 뺄셈 원리 (소)
│   └── 구구단 패턴 (소)
└── 분수와 소수 (중)
    └── ...
```
- **왼쪽**: 트리 구조로 전체 개념 표시 (클릭하여 선택)
- **오른쪽**: 선택된 개념의 상세정보 + 수정/삭제/하위추가 버튼
- **+ 개념 추가**: 이름, 레벨(대/중/소), 상위 개념, 학년 범위, 정렬순서

### `/admin/problems` 문제 관리
- 문제 목록을 테이블로 표시 (수식, 정답, 유형, 난이도)
- **+ 문제 추가** 다이얼로그:
  - 수식 입력 (KaTeX 문법) → 실시간 미리보기
  - 정답, 답 유형 (숫자/수식/선택), 난이도 (1~5)
  - 힌트 (선택)
  - **개념 태그**: 소개념 목록에서 클릭하여 다대다 연결
  - 주요 개념 선택 (is_primary)

### `/admin/sequences` 시퀀스 관리
- 시퀀스 목록 + 미리보기 펼치기
- **+ 시퀀스 추가** 다이얼로그:
  - 이름, 설명
  - **왼쪽**: 전체 문제 목록 (클릭하면 추가)
  - **오른쪽**: 시퀀스에 넣은 문제 (↑↓로 순서 변경, ×로 제거)
  - 순서가 핵심! 학생이 이 순서대로 문제를 풀게 됩니다

---

## 3. 교사 대시보드 (👨‍🏫 교사로 로그인)

### `/dashboard` 메인
- 통계 카드: 내 반 수, 총 학생 수, 총 제출 수
- 빠른 이동 버튼 (반 관리, 시퀀스 배정, 학생 분석)
- 최근 배정 목록
- 내 반 목록 (카드형)

### `/dashboard/classes` 반 관리
- 반 목록 (카드, 학생 수 표시)
- **+ 반 만들기**: 반 이름 입력

### `/dashboard/classes/[id]` 반 상세
- 반 이름, 학생 수
- 학생 테이블: 이름, 이메일, 평균 마스터리
- **+ 학생 추가**: 학생 이메일로 검색하여 추가
- 학생 제거

### `/dashboard/concepts` 개념 트리 (읽기전용)
- 전체 개념 트리를 트리 뷰로 탐색

### `/dashboard/sequences` 시퀀스 목록
- 전체 시퀀스를 카드로 표시 (문제 수 뱃지)
- 클릭하면 상세 페이지로

### `/dashboard/sequences/[id]` 시퀀스 상세
- 시퀀스 이름, 설명
- 문제 목록: 순서 번호 + KaTeX 수식 + 정답 + 난이도

### `/dashboard/assign` 시퀀스 배정
- **반 선택** (내가 만든 반 목록)
- **시퀀스 선택** (전체 시퀀스 목록)
- **마감일** (선택)
- 배정 버튼 → 해당 반의 모든 학생에게 시퀀스가 배정됨

### `/dashboard/analytics` 학생 분석
- 반 선택 드롭다운
- 학생별 테이블: 이름, 시도 수, 정답 수, 정확도(프로그레스바), 평균 시간

---

## 4. 학생 페이지 (👨‍🎓 학생으로 로그인)

### `/learn` 내 학습
- 배정된 시퀀스 목록 (카드)
- 각 카드에: 시퀀스 이름, 반 이름, 진행률 바, 마감일
- 버튼: 시작하기 / 이어서 풀기 / 다시 보기 (완료 시)

### `/learn/[assignmentId]` 문제 풀기 (핵심 UX)
```
┌──────────────────────────────────┐
│  시퀀스: "곱셈에서 인수분해로"      │
│  진행: ████████░░ 8/12            │
├──────────────────────────────────┤
│                                  │
│         9 × 111 = ?              │  ← KaTeX 렌더링
│                                  │
│         [ 답 입력 ] [제출]        │
│                                  │
│              [힌트]              │
├──────────────────────────────────┤
│  O  9×11 = 99                    │  ← 정답 이력
│  O  9×111 = 999                  │
│  X  9×1111 (오답: 9998)          │  ← 오답 이력
└──────────────────────────────────┘
```
- 수식 하나 + 입력칸 하나의 집중된 UI
- 정답 → 다음 문제로 자동 진행
- 오답 → 같은 문제 재시도 (히스토리에 오답 기록)
- **이전 풀이 히스토리**가 아래에 쌓임 → 패턴 발견 유도
- **힌트 버튼** → Claude AI가 최소한의 힌트 제공
- 모든 문제 완료 시 → 완료 화면 + 전체 히스토리

### `/learn/review` 오답 복습
- 아직 맞추지 못한 문제만 모아서 표시
- 각 문제: 수식 + 시도했던 오답 목록 + 힌트

### `/progress` 내 진도
- 통계 카드: 총 시도, 정답, 평균 마스터리, 마스터 개념 수
- 정확도 프로그레스 바
- **개념별 마스터리 트리**: 개념 트리에 각 개념의 습득도(%)를 색상 바로 표시
  - 80%+ 초록 / 50%+ 노랑 / 20%+ 주황 / 20%미만 빨강

---

## 5. 핵심 시스템

### 마스터리 계산
```
마스터리 = (정답 수 / 시도 수) × min(시도 수 / 5, 1)
```
- 정확도 기반 + 시도 횟수에 따른 신뢰도 가중치
- 5문제 미만이면 마스터리가 보수적으로 계산됨
- 문제를 풀 때마다 연결된 개념의 마스터리 자동 업데이트

### AI 튜터 (Claude API)
- `/api/hint` 엔드포인트
- 규칙: 정답을 절대 알려주지 않음, 한두 문장, 풀이 방향만 제시
- 학생의 이전 시도를 컨텍스트로 제공

### 인증 흐름
```
미인증 → /dashboard, /learn, /admin → 302 → /login
인증됨 → /login, /signup → 302 → /dashboard 또는 /learn
```

---

## 6. 데이터 흐름 요약

```
관리자: 개념 트리 생성 → 문제 생성 + 개념 태깅 → 시퀀스 조립
                                                      ↓
교사:  반 생성 → 학생 등록 → 시퀀스 배정 ─────────────→ 배정
                                                      ↓
학생:  배정 확인 → 문제 풀기 → 제출 기록 → 마스터리 업데이트
                                   ↓
교사:                          분석 확인
```

---

## 7. 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | Next.js 14 (App Router) + TypeScript |
| UI | Tailwind CSS + shadcn/ui |
| 수식 렌더링 | KaTeX |
| Backend/DB | Supabase (PostgreSQL + Auth + RLS) |
| AI 튜터 | Claude API (Anthropic) |
| 상태 관리 | React hooks (useSequencePlayer, useConceptTree) |

---

## 8. 폴더 구조

```
src/
├── app/                          # 페이지 (App Router)
│   ├── page.tsx                  # 랜딩
│   ├── login/ signup/            # 인증
│   ├── admin/                    # 관리자 (개념, 문제, 시퀀스)
│   ├── dashboard/                # 교사 (반, 배정, 분석)
│   ├── learn/                    # 학생 (문제 풀기, 복습)
│   ├── progress/                 # 학생 진도
│   └── api/hint/                 # AI 힌트 API
├── components/
│   ├── ui/                       # shadcn/ui 컴포넌트
│   ├── math/                     # KaTeX, 문제카드, 답입력, 시퀀스플레이어
│   ├── concept/                  # 개념트리, 마스터리뱃지
│   └── layout/                   # Header, Sidebar, 레이아웃
├── hooks/                        # useSequencePlayer, useConceptTree
├── lib/                          # Supabase 클라이언트, 마스터리, AI튜터, 테스트인증
└── types/                        # TypeScript 타입 정의

supabase/migrations/
└── 001_initial.sql               # DB 스키마 (11 테이블 + RLS)
```
